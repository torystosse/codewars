function numberToString(num) {
  // store num in variable
  const newNum = num
  // use JS built in .toString() method on above variable, store in new variable
  const numString = newNum.toString()
  // return numString variable
  return numString
}
